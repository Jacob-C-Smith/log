name: log

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install Dependencies
      run: |
        sudo apt update  &&
        sudo apt upgrade &&
        sudo apt install make &&
        sudo apt install valgrind &&
        git submodule update --remote
    - name: Build
      run: |
        cd ${{github.workspace}}
        make all
    - name: Test
      working-directory: build
      run: |
        cd ${{github.workspace}}/build && ./build/log_example

